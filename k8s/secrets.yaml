# Secret template — DO NOT commit real values.
# Generate and apply with:
#   kubectl create secret generic docuflux-secrets \
#     --from-literal=secret-key="$(openssl rand -hex 32)" \
#     --from-literal=redis-password="$(openssl rand -hex 24)" \
#     -n docuflux
#
# Or use sealed-secrets / external-secrets for GitOps:
#   https://github.com/bitnami-labs/sealed-secrets
apiVersion: v1
kind: Secret
metadata:
  name: docuflux-secrets
  namespace: docuflux
type: Opaque
stringData:
  secret-key: REPLACE_ME
  redis-password: REPLACE_ME
