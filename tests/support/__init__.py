"""Shared test support: heavy-dependency stubs, fixtures, and data factories.

Import fixtures from :mod:`tests.support.fixtures` into a test module to use them;
import the builders in :mod:`tests.support.factories` directly as plain functions.
"""
