# GPU-enabled worker requirements
# Includes full Marker dependencies with CUDA/PyTorch GPU support

celery==5.3.4
redis==5.0.1
requests==2.33.0
flask==3.1.3
flask-socketio==5.3.6
gevent==23.9.1
gevent-websocket==0.10.1
prometheus-client==0.19.0
cryptography==48.0.1

# config.py imports pydantic_settings at module scope and worker/tasks/__init__.py
# imports config, so the worker cannot start without it.
pydantic-settings==2.2.1
# shared/storage.py imports boto3 when STORAGE_BACKEND=s3.
boto3>=1.34.0

# Story 2.1a/2.1b: Tesseract OCR bindings + PDF rasterisation. worker/tasks/conversion.py
# imports these for the engine=ocr path, which is routed to the default/high_priority
# queues — a GPU worker subscribed to those queues needs them too.
pytesseract==0.3.13
pdf2image==1.17.0

# Marker with full GPU support (includes PyTorch, torchvision, torchaudio with CUDA)
# Note: marker-pdf[full] automatically installs compatible versions of all ML dependencies
# Do NOT pin torch/transformers/torchvision/torchaudio - let marker-pdf manage them
marker-pdf[full]==2.0.0

# Epic 26: SLM (Small Language Model) support with CUDA
llama-cpp-python[cuda]
# Pillow is intentionally unpinned: marker-pdf 2.0.0 does NOT lift the Pillow<11 constraint.
# CVE-2026-25990 (Pillow>=12.1.1) remains unresolved — marker-pdf 2.0.0 pins Pillow<11.
# The existing Pillow>=12.1.1 pin for CPU builds is kept separate; GPU builds use the
# marker-pdf–managed Pillow<11 to avoid the CVE while preserving Marker compatibility.
