# CPU-only worker requirements
# Minimal dependencies without GPU/CUDA support

celery==5.3.4
redis==5.0.1
requests==2.33.0
flask==3.1.3
flask-socketio==5.3.6
gevent==23.9.1
gevent-websocket==0.10.1
prometheus-client==0.19.0

# shared/encryption.py, shared/key_manager.py and shared/redis_encryption.py import
# cryptography unconditionally — without it the CPU image cannot import shared/ at all.
cryptography==50.0.0
# config.py imports pydantic_settings at module scope and worker/tasks/__init__.py
# imports config, so the worker cannot start without it.
pydantic-settings==2.2.1
# shared/storage.py imports boto3 when STORAGE_BACKEND=s3.
boto3>=1.34.0

# Note: Marker is NOT installed in CPU-only mode
# The worker will detect GPU unavailable at runtime and skip Marker tasks
# This significantly reduces image size from ~15GB to <3GB

llama-cpp-python
Pillow>=12.1.1  # CVE-2026-25990: out-of-bounds write in PSD image loading

# Story 2.1a/2.1b: Tesseract OCR bindings + PDF rasterisation for the CPU
# OCR path. The tesseract engine + language packs are installed at the system
# level in worker/Dockerfile (TESSERACT_LANGS build arg).
pytesseract==0.3.13
pdf2image==1.17.0

